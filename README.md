# NightWatch Knowledge Commons

> 공개 티어. **정산되는 현실**(거래소 API·온체인·실송금)로 채점 가능한 시장구조 지식만 담는다 — 수수료·입출금 문·체인·계정 모드·시장 실측.
> 1파일 = 1노드, `[[링크]]` = 엣지. Obsidian으로 열면 그래프가 나온다. AI가 읽는 법은 [[CLAUDE]].
> 모든 수치에는 `as_of`가 붙어 있다. **행동 전에 [[INDEX]]의 신선도 표를 먼저 보라** — 어제의 최저 회랑이 오늘도 최저라는 보장은 없다. 라이브 값은 NightWatch API/MCP.
> English edition: [[en/README]].

원자 노트(1사실=1노트)는 각 허브 노드 끝 `## Atoms`에 링크되어 있다. 규약 = [[ATOMIZATION_SPEC]].
게이트(행동 규칙)·교훈 심층·펀드 내부는 이 저장소에 없다 — 미해결 링크는 그 티어를 가리킨다.

기여: 사실을 고치지 않는다 — 새 `as_of` 항목을 추가한다. 출처 없는 사실은 받지 않는다.
